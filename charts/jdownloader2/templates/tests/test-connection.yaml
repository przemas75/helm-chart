apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jdownloader2.fullname" . }}-test-connection"
  labels:
    {{- include "jdownloader2.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jdownloader2.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
